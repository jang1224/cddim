Data Set : RESIDE-b

epoch : 300 -> forced termination -> so only generated 16 images

steps_per_epoch : 750

val_steps_per_epoch : 100

batch : 16 * 2 -> gradient accumulation

image size : 128

widths : [64,128,256,256]

block_depth : 2

---

only change Data Set -> RESIDE-6K to RESIDE-b
